<?php
/*
<script>
// RELOADS MASONRY ITEMS POSITION WHEN MOBILE ORIENTATION CHANGES
// if the window is smaller than ipad (to exclude the tablets), leaving only mobile phones affected, at lease most are. jQuery should be replaced for the $ sign because for some reason it is not defined.
if (jQuery(window).width() < 700) {
	window.onorientationchange = function() {
		var $masonry = $('#masonry');
		var orientation = window.orientation;
			switch(orientation) {
				case 0: $masonry.masonry('reloadItems');
				break;
				case 90: $masonry.masonry('reloadItems');
				break;
				case -90: $masonry.masonry('reloadItems');
				break; }
	};
};
</script>
*/
